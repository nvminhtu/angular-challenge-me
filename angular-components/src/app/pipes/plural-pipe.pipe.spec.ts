import { PluralPipePipe } from './plural-pipe.pipe';

describe('PluralPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PluralPipePipe();
    expect(pipe).toBeTruthy();
  });
});
